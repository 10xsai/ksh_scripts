#!/usr/bin/ksh

# Defining some variables
a=16; print "a is $a"
b=9; print "b is $b"
c=25; print "c is $c"

# Addition of three numbers
((Result=a+b+c))
print "Result of adding three numbers is ${Result}"